image: node:alpine

stages:
  - build

react_build:
  stage: build
  tags:
    - iprofiler
  before_script:
    - echo "Installing NPM Dependencies and building React app"
    - cd react-app
  script:
    - npm install
    - npm run build
  after_script:
    - echo "Installation and build successful"
  cache:
    key: install-cache
    paths:
      - node_modules/
  artifacts:
    paths:
      - build/
